services:
  - type: web
    name: mindguard
    env: python
    runtime: python
    buildCommand: "apt-get install -y libpango-1.0-0 libgdk-pixbuf2.0-0 libffi-dev && pip install -r requirements.txt"
    startCommand: "gunicorn main:app --bind 0.0.0.0:$PORT"
